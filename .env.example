# =============================================================================
# Daily Deep Brief - 環境變數設定
# =============================================================================
# 複製此檔案為 .env 並填入實際值

# -----------------------------------------------------------------------------
# 資料供應商 API Keys
# -----------------------------------------------------------------------------
# Financial Modeling Prep API (https://financialmodelingprep.com/)
FMP_API_KEY=your_fmp_api_key_here

# Alpha Vantage API (https://www.alphavantage.co/)
ALPHAVANTAGE_API_KEY=your_alphavantage_api_key_here

# -----------------------------------------------------------------------------
# Ghost CMS 設定
# -----------------------------------------------------------------------------
# Ghost Admin API URL (例如 https://your-site.ghost.io)
GHOST_API_URL=https://your-ghost-site.ghost.io

# Ghost Admin API Key (格式: {id}:{secret})
# 在 Ghost Admin -> Settings -> Integrations -> Add custom integration
GHOST_ADMIN_API_KEY=your_ghost_admin_api_key_here

# Ghost API 版本 (依你的 Ghost 版本設定)
GHOST_ACCEPT_VERSION=v5.0

# Ghost Newsletter slug (用於寄送 newsletter)
GHOST_NEWSLETTER_SLUG=default-newsletter

# -----------------------------------------------------------------------------
# Ghost 安全 Allowlist (強烈建議設定，防止誤寄)
# -----------------------------------------------------------------------------
# 允許的 Newsletter slugs (逗號分隔)
# 例如: daily-brief-test,daily-brief
GHOST_NEWSLETTER_ALLOWLIST=daily-brief-test

# 允許的 Email segments (逗號分隔)
# 例如: label:internal,status:free
GHOST_SEGMENT_ALLOWLIST=label:internal

# -----------------------------------------------------------------------------
# LLM 模型設定
# -----------------------------------------------------------------------------
# 使用的 AI 模型 (支援 Claude, Gemini, GPT 等)
# Claude: claude-sonnet-4-20250514, claude-opus-4.5
# Gemini: gemini-3-pro-preview, gemini-3-flash-preview
# GPT: gpt-4o, gpt-5, gpt-5.1, gpt-5.2
CODEX_MODEL=gemini-3-flash-preview

# LiteLLM Proxy 設定 (用於非 Claude 模型)
LITELLM_BASE_URL=https://litellm.whaleforce.dev
LITELLM_API_KEY=your_litellm_api_key_here

# -----------------------------------------------------------------------------
# 執行設定
# -----------------------------------------------------------------------------
# 運行模式: draft / publish
RUN_MODE=draft

# Edition: premarket / postclose / intraday
EDITION=postclose

# 時區
TIMEZONE=America/New_York

# -----------------------------------------------------------------------------
# 告警設定 (選填)
# -----------------------------------------------------------------------------
# Slack Webhook URL
SLACK_WEBHOOK_URL=

# Email 告警 (SMTP)
ALERT_EMAIL_TO=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# -----------------------------------------------------------------------------
# 快取與儲存
# -----------------------------------------------------------------------------
# 快取 TTL (秒)
CACHE_TTL_SECONDS=600

# 資料庫路徑 (SQLite)
DATABASE_PATH=data/cache.db

# -----------------------------------------------------------------------------
# 日誌設定
# -----------------------------------------------------------------------------
LOG_LEVEL=INFO
LOG_FORMAT=json
